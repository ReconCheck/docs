# 跨验证引擎的「作业层」定位

> 本文件说明 ReconCheck 在更大场景中的位置：引擎层负责「把两份单据对齐并判定」，作业层负责「管理一批单据的核对这件事」。引擎可以换来换去，作业层是稳定的入口。

## 问题：核对引擎有很多种，企业只想要一个入口

现实中的比对引擎五花八门：

- 规则型引擎（差分规则 + 容差/例外，如 ReconCheck 当前的判定器）
- 机器模型（相似度打分、异常检测）
- 第三方 OCR + 对齐服务（不同供应商的版面还原与实体解析）

企业财务/供应链不想为每一家引擎各写一套流程。他们要的是：**把一批单据交进去，拿到按单据对组织的核对结果，能复核、能归档**。至于背后是哪个引擎，是可替换的实现细节。

## 分层

```
┌─────────────────────────────────────────────────┐
│  作业层 (Job Layer)                              │
│  单据入库 · 批次 · 配对 · 执行 · 结果归档 · 复核  │
│  引擎无关：Document / Pair / Report 三个抽象      │
├─────────────────────────────────────────────────┤
│  引擎层 (Engine Layer)                           │
│  Parse → Align → Judge → Cite                   │
│  ReconCheck core、规则库、第三方引擎适配器…      │
└─────────────────────────────────────────────────┘
```

- **引擎层**回答「这两份单据，哪里对不上、差多少、证据在哪一行」。ReconCheck core 就是引擎层的一个实现，接口契约是报告 JSON（每条 finding 带双侧 `cell://` 证据）。
- **作业层**回答「这一批单据核对得怎么样了」。它管理生命周期：单据对象（Document）→ 配对单元（Pair）→ 执行 → 结果（Report）→ 归档/复核；作业本身不关心用的是哪个引擎。

## ReconCheck 的现状：作业层语义已经成形（v0）

`/api/jobs`、`/api/documents`、`/api/reports` 三者合起来就是作业层的 v0 实现，全部接口与具体引擎无关：

| 作业层抽象 | ReconCheck v0 载体 |
|---|---|
| 单据对象 Document | 文档库（上传文件 /records JSON /数据源拉取），统一身份 `doc_id` |
| 配对单元 Pair | `/api/jobs` 内按业务键自动生成的 `(left, right)`；每组独立状态（done/failed）+ 独立报告 |
| 结果 Report | `/api/reports/{id}` 归档 JSON，二次可拉取、可深链 |
| 批次 Batch | 一次 `/api/jobs` 提交 = 一个批次；逐对进度轮询 |
| 复核/归档 | 报告的 evidence 双侧坐标 + 前端点击回原文（复核入口） |

作业层的非功能约束同样已落地：持久化（`RECONCHECK_DATA`）、重启恢复 queued 批次、TTL 归档清理、失败对隔离、上传/拉取上限、只读审计。

## 未来：同一作业层接多个引擎

1. **适配器**：把作业层的 `Pair` 结果描述成标准契约（对齐行数、finding 列表、证据坐标），第三方引擎实现适配器即可接入同一作业层 UI 与结果流。
2. **引擎选择**：作业提交时声明引擎（或按文件类型/单据种类路由），`/api/jobs` 配置项扩展为引擎选择器，不改作业层主体。
3. **混合核对**：规则型引擎出确定性判定，模型/大模型参与消歧与解释——这正是 LLM 设计（见 core `DESIGN.md`）与本文的分工：**LLM 是引擎层的一根管子，不是作业层**。

约束保持不变：对所有引擎一律只读上游、evidence 契约一致、判定可追溯。

## 一句话

引擎决定「差多少」，作业层决定「这一批查得怎样」。ReconCheck 现在把作业层也做出来了，未来引擎可以被替换、被组合，作业入口不动。